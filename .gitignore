# binarios / build de Go
*.exe
*.test
*.out
/bin/

# descarga original redundante (ya está extraída en tutorial-original/)
tutorial-original.zip

# editor / SO
.DS_Store
*.swp
*~

# binario compilado del daemon (nombre = módulo)
/astro

# secreto local (API key) — nunca versionar
secrets.env
